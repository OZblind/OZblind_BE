from django.urls import path
from . import views

urlpatterns= [
    # path('auth/social/google/signup', views.GoogleSignUpView.as_view(), name='google-signup'),
    # path('auth/social/google/login', views.GoogleLoginView.as_view(), name='google-login'),
]